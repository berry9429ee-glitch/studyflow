<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyflow.mapper.PlanLogMapper">
    <resultMap id="PlanLogResultMap" type="com.studyflow.entity.PlanLog">
        <id column="id" property="id"/>
        <result column="plan_id" property="planId"/>
        <result column="user_id" property="userId"/>
        <result column="action" property="action"/>
        <result column="detail" property="detail"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="selectRecentByPlanId" resultMap="PlanLogResultMap">
        SELECT id, plan_id, user_id, action, detail, created_at
        FROM plan_logs
        WHERE plan_id = #{planId}
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="selectWeeklyDone" resultType="java.util.Map">
        SELECT DATE(created_at) AS date, COUNT(*) AS count
        FROM plan_logs
        WHERE user_id = #{userId}
          AND action IN ('ITEM_DONE', 'STATUS_CHANGED_DONE')
          AND created_at &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE(created_at)
        ORDER BY DATE(created_at) ASC
    </select>
</mapper>
